'use strict';

const expect = require('chai').expect;
const Client = require('../lib/client');

describe('Client()', () => {

  describe('constructor()', () => {

    it('should...', () => {
    });

  });

});
